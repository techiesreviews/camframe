# Verification run: v0.4.3 documentation baseline — 2026-08-28

- **Commit:** `fe8e6c6882fd874f41050e4ac2e90e32ad398e4b`
- **Version:** `0.4.3`
- **Observer:** Codex documentation audit in T3 Code
- **Environment:** Windows PowerShell; Node `v24.15.0`; npm `11.14.1`; Electron `43.2.0`; Electron Builder `26.15.3`; Vite `8.2.0`
- **Cameras:** No hardware/manual camera run performed
- **Scope:** Documentation baseline, not a release sign-off

## Automated checks

| Command | Result | Counts/duration | Evidence/notes |
| --- | --- | --- | --- |
| `npm.cmd test` | Pass | 40/40, 0 failed, Node reported ~109 ms | Pure and static contract suite |
| `node --check` for all six JavaScript modules and `preload.cjs` | Pass | No diagnostics | Explicit checks run for main, Overlay, Controller, settings, cameras, fullscreen, and preload |
| `git diff --check` before and after documentation edits | Pass | No errors | Git emitted expected LF→CRLF working-copy notices on this Windows clone |
| `npm.cmd ls --depth=0` | Pass | 3 top-level packages | Resolved versions recorded above |
| Local Markdown link-resolution check | Pass | All relative links resolve | Checked all repository Markdown outside dependencies |

`npm ci` was not run because the existing lockfile installation was already present; this run does not prove a clean install.

## Build and renderer smoke attempts

| Target/check | Result | Observation |
| --- | --- | --- |
| `npm.cmd run pack` | Inconclusive | Electron Builder started but produced no further output and no newly timestamped artifact after more than three minutes; interrupted with Ctrl+C. Pre-existing `dist/win-unpacked` is dated 2026-08-24 and was not credited to this run. |
| `npm.cmd run dev -- --host 127.0.0.1` | Inconclusive | Vite process started but emitted no ready URL and opened no listening port during the observation window; interrupted with Ctrl+C. No browser visual QA was claimed. |
| Windows NSIS/portable | Not run | `dist` command not attempted after unpacked-build result |
| macOS arm64/x64 | Not run | Windows environment |
| Artifact contents/hashes | Not run | Existing output is stale relative to this audit |

## Manual checks

All W-01–W-27 and M-01–M-09 checks are **Not run**. In particular, this audit does not claim real-camera continuity, Elgato behavior, transparent hit testing, taskbar z-order, Full screen visual quality, shortcuts, Start at login, or installer success.

## Source/history findings

- The current implementation and all 40 tests were traced module by module.
- Merged PR descriptions for PRs 1–10 (available set: 1–4 and 6–10), first-parent tags v0.2.12–v0.4.3, all accepted ADRs, and GitHub issue 5 were inspected.
- Native-region helpers are not connected to `main.js`/`BrowserWindow.setShape()`.
- The separate Controller is implemented but `createControlWindow()` has no caller.
- GitHub issue 5 preserves a failed canvas workaround and unresolved manual evidence against seamless dynamic profile switching.

These findings are documented in `known-gaps.md`; they are not code fixes.

## Release decision

- **Decision:** Documentation baseline only
- **Reason:** Automated source-level checks pass, but build smoke attempts were inconclusive and no OS/hardware manual matrix was run.
- **Follow-ups:** Rerun from clean install; diagnose pack/Vite startup separately if reproducible; execute Windows hardware matrix before calling v0.4.3 fully verified.
